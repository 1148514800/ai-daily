# AGENTS.md

这些规则适用于整个 `ai-daily` 仓库。

## 开始任务前

- 开始任务前先阅读 `AGENTS.md` 和 `AI_HANDOFF.md`
- 将 `AI_HANDOFF.md` 视为当前项目状态，而不是成长日志

## 工程原则

- 优先保持代码简单、清晰、可维护
- 不为了“架构完整”提前引入复杂组件
- 不提前增加微服务、消息队列、向量数据库、多 Agent
- 每个 Phase 只修改当前阶段必要内容

## 安全与配置

- 不在客户端保存 API Key
- 所有敏感配置放入环境变量
- 不提交 `.env`
- 不未经允许修改已经稳定的接口

## 测试与交付

- 新增功能必须尽量带测试
- 完成任务后更新 `AI_HANDOFF.md`
- 每个 Phase 完成后创建独立 Git commit
- 未经明确要求不要 push
